spring:
  profiles:
    active: local   # 현재 local 프로필만 활성화

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    jdbc-url: jdbc:mysql://localhost:3306/dicom_app?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

---
spring:
  config:
    activate:
      on-profile: oracle
app:
  datasource:
    oracle:
      url: jdbc:oracle:thin:@210.94.241.9:1521/xe
      username: C##sahmyook
      password: sahmyook1
      driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    oracle:
      ddl-auto: validate
      show-sql: false
      properties:
        hibernate.dialect: org.hibernate.dialect.OracleDialect

---
spring:
  config:
    activate:
      on-profile: local

jwt:
  secret: fd471a4de6ec54a281f66c51c264a2e7  # ✅ JWT 서명용 비밀키
  expiration: 3600000                      # ✅ 토큰 만료시간 (1시간 = 3600000ms)
